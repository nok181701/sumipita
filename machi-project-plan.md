# スムピタ プロジェクト構想メモ

## コンセプト
東京23区の治安・浸水リスク・地盤・人口動態を、住所や町丁目単位で可視化する引越し先リサーチダッシュボード。
警視庁の犯罪情報、国交省のハザードマップ、地盤データなどバラバラに散らばる公的データを1つのスコアカードに統合し、
「この街に住んで大丈夫か」を一目で判断できるようにする。

## 対象エリア
東京23区（データの粒度・管轄が揃うため）

## 使いたい主なデータソース
- **人口・世帯**: 東京都「東京都の統計」、e-Stat（東京都・区単位）
- **治安**: 警視庁「犯罪情報マップ」（町丁目単位、CSVダウンロード可）
- **浸水・洪水リスク**: 国交省「重ねるハザードマップ」のGISデータ、東京都建設局の洪水浸水想定区域図
- **地盤**: 東京都土木技術支援・人材育成センター「東京地盤」（ボーリングデータ）
- **地価**: 国土数値情報 地価公示

## MVP構成案
1. 区・町丁目を選択、または住所検索
2. スコアカード表示: 治安スコア／浸水リスク／地盤の強さ／人口動態トレンド
3. 地図上にハザードレイヤーを重ねて表示

## 技術構成
- **フロントエンド**: Next.js
- **DB / サーバー処理**: Cloudflare（D1でスコアなどの構造化データ、R2で生のGeoJSON/シェープファイルを保存）
- **バックエンド（データ取得バッチ）**
  - 重いETL処理（シェープファイル→GeoJSON変換、空間結合など）: GitHub Actionsの定期実行でPython + geopandas/shapely → 処理後のデータをD1/R2に書き込み
  - 軽い定期更新（人口統計の再取得など）: Cloudflare Workers + Cron Triggers
- **CI/CD**: GitHub Actions

## リリース方針
1. まずWebアプリとしてリリース
2. その後ネイティブアプリを開発

## アプリ名
スムピタ（「住む」+「ピタッと（フィットする）」— 自分に合う街がピタッと見つかるイメージ）

---

## 犯罪データ調査結果（2026-08-06時点）

### 採用データ
警視庁「区市町村の町丁別、罪種別及び手口別認知件数」令和7年（R7.csv）
- ダウンロードURL: `https://www.keishicho.metro.tokyo.lg.jp/about_mpd/jokyo_tokei/jokyo/ninchikensu.files/R7.csv`
- 年度ごとに `R7.csv` / `R6.csv` の命名規則。令和8年は月次更新版（`R8.csv`、`R8.4.csv` 等）もあり
- **ライセンス: CC BY**（東京都オープンデータカタログサイトのデータセットページに明記）。出典表示すれば商用利用・再配布可

### ファイル仕様
- **文字コード: Shift_JIS（CP932）** — 読み込み時に `encoding='cp932'` 必須
- 38列 × 約5,260行。**R6とR7でカラム構成は完全一致**（年度またぎのバッチ処理が可能）
- 識別子は `市区町丁` の1列のみ。区名と町丁目名が連結されている（例: `千代田区飯田橋１丁目`）
- 残り37列はすべて件数。罪種は「大分類計 + 内訳」の階層構造
  - 凶悪犯（強盗・その他）
  - 粗暴犯（凶器準備集合・暴行・傷害・脅迫・恐喝）
  - 侵入窃盗（金庫破り・学校荒し・事務所荒し・出店荒し・空き巣・忍込み・居空き・その他）
  - 非侵入窃盗（自動車盗・オートバイ盗・自転車盗・車上ねらい・自販機ねらい・工事場ねらい・すり・ひったくり・置引き・万引き・その他）
  - その他（詐欺・占有離脱物横領・その他知能犯・賭博・その他刑法犯）

### パース時の注意点
1. **丁目が全角数字**（`１丁目`）。R7で2,910件が該当。GeoJSONの町丁目ポリゴンと突合する際は正規化が必須
2. **除外すべき行**（すべて `市区町丁` 列で判定）
   - `〜計`（`千代田区計` など各区の集計行、`多摩地区・島部計`）
   - `〜以下不詳`（`中央区以下不詳` など。"計"を含まないのでフィルタをすり抜ける）
   - **区名のみと完全一致する行**（`新宿区` など末尾に23件。これも"計"を含まずすり抜ける）
   - `他県` / `海外認知` / `不明` / `合計`
3. **年度間で町丁目が増減する**（R7のみ28件、R6のみ18件）。埋立地の新町名や町名変更が原因。マスタは年度ごとに持つ設計が安全

### 23区の実データ（令和7年）
- 有効な町丁目数: **3,137件**（除外行を除いた実質数）
- 23区合計: 147,722件（R6は140,162件）

罪種大分類の構成比:

| 大分類 | 件数 | 比率 |
|---|---|---|
| 非侵入窃盗 | 87,961 | 59.5% |
| その他 | 40,049 | 27.1% |
| 粗暴犯 | 14,430 | 9.8% |
| 侵入窃盗 | 3,688 | 2.5% |
| 凶悪犯 | 1,587 | 1.1% |

主要手口: 自転車盗 38,266 / 万引き 17,592 / 暴行 8,062 / 傷害 4,932 / 車上ねらい 2,446 / 置引き 1,522 / 空き巣 1,102 / 強盗 404 / ひったくり 136

### スコアリング方針（確定版・2026-08-06 改訂）

**罪種グループと重み**

| グループ | 対象手口 | 重み |
|---|---|---|
| 重大犯罪 (serious) | 強盗・凶悪犯その他・暴行・傷害・脅迫・恐喝・凶器準備集合 **＋ 空き巣・忍込み・居空き** | 65% |
| 生活犯罪 (daily) | 自転車盗・車上ねらい・ひったくり・すり・置引き・オートバイ盗・自動車盗 | 35% |

侵入窃盗は**住宅対象の3手口のみ**（事務所荒し/出店荒し/学校荒しは除外。引越し先選びではオフィスビルの被害は関係ないため）。

**スコアから除外した罪種**: 万引き・賭博・占有離脱物横領・詐欺
→ 商業施設の有無で決まり、住民の体感治安と無関係。自転車盗38,266件・万引き17,592件が全体を支配してしまうため。
（実測: 罪種選別なしの単純合算スコアとは**90ポイント以上乖離する町丁目が複数ある**。選別していなければ結論が真逆になっていた）

**正規化の方法**
- 分母は住民基本台帳の人口（人口1000人・1年あたり発生率）
- Zスコアは使わない（中央値14件に対し最大6,977件と分布が極端に歪んでいるため）
- パーセンタイル順位を使用。`rank(method='max', ascending=False, pct=True) * 100`
  - 「自分以上の発生率の町丁目が何%あるか」
  - **`method='min'` だと0件グループが同着で中央順位に潰れ、最高点が77点止まりになる**（ハマりどころ）
- **人口100人未満の町丁目はスコア対象外**（148件該当）。分母が小さすぎて発生率が発散するため

**重大犯罪は2年合算（R6 + R7）**
希少事象なので1年だと0件の町丁目が多すぎて指標にならない。

### 【改訂】希少事象の扱い — 3つの修正

初版の設計には、**重大犯罪1件でスコアが急落する**という実害のある欠陥があった。

修正前の実測:

| 重大犯罪 | 町丁目数 | 治安スコア中央値 |
|---|---|---|
| 0件 | 966 | **85.0** |
| 1件 | 763 | **58.6** |

0件→1件で**26ポイント急落**。世田谷区玉川田園調布2丁目は年間犯罪が暴行1件のみ（人口1,275人）で44点となり、
単純件数ベースなら99点相当の町丁目が「平均以下」判定されていた。

**修正① 重大犯罪も2年合算に**（侵入窃盗と同じ処置）

**修正② ベイズ平滑化を導入。ただし擬似「人口」ではなく擬似「件数」を固定する**
```python
prior_person_years = PRIOR_EVENTS / r0     # r0 = 23区全体の発生率
rate = (count + PRIOR_EVENTS) / (person_years + prior_person_years) * 1000
```
PRIOR_EVENTS = 2.0

擬似人口を一律で決めると、頻度の高い生活犯罪まで平均に引き寄せられて**「0件」という有力な情報が消える**。
擬似件数を固定して擬似人口を発生率から逆算すれば、希少な罪種ほど強く平滑化され、頻繁な罪種は素の値を保つ。

**修正③ 侵入窃盗を重大犯罪に統合（決定打）**

侵入窃盗を単独グループにすると**73%が0件**になり、スコアが断崖状になっていた:

| 侵入窃盗(2年) | 町丁目数 | 侵入窃盗スコア中央値 |
|---|---|---|
| 0件 | 2,181 | **63.5** |
| 1件 | 568 | **17.7** |

0件と1件の間に46ポイントの断崖 = 実質的に「空き巣があったか否か」の2値指標。
暴力犯罪と統合すると**0件は13%まで下がり、中央値3件の連続分布**になる。
「住まいの安全に直結する犯罪」というくくりとしても意味が通る。
内訳（空き巣が何件か等）は生件数として `crime_counts` テーブルで別途表示する。

**修正後の実測**

| 重大犯罪(2年) | 修正前 | 修正後 |
|---|---|---|
| 0件 | 85.0 | 77.9 |
| 1件 | 58.6 | **68.8** |
| 2件 | 49.7 | 58.3 |
| 3件 | 43.2 | 53.2 |
| 10件 | — | 19.7 |

0件→1件の落差が**26→9ポイント**に縮小し、件数に応じてなだらかに低下する形に。
スコアの標準偏差は21.5→**24.7**に広がり、識別力はむしろ向上。

### 算出結果（改訂後）
- 町丁目3,142件中、**2,994件でスコア算出可**
- スコア分布: 中央値51.4、四分位31.2〜69.9、最大99.1
- 下位: 歌舞伎町1丁目（重大310件）、西池袋1丁目、道玄坂2丁目、外神田一丁目 → 繁華街が正しく並ぶ

**区別平均が大きく変動した**（平滑化により「重大犯罪0件の小さな町丁目」が区平均を押し上げなくなったため）:

| 区 | 改訂前 | 改訂後 |
|---|---|---|
| 千代田区 | 56.5 | **28.5** |
| 港区 | 50.3 | **33.7** |
| 新宿区 | 49.9 | **32.4** |
| 台東区 | 47.1 | 32.9 |
| 渋谷区 | 42.3 | 31.6 |

> **※ただしこの区平均はUIに出さないことにした。**
> 千代田区の28.5は夜間人口を分母にしたことによる指標の欠陥であり、実際の危険度ではない。
> 詳細は後述の「区平均は廃止。夜間人口を分母にした指標の限界」を参照。

### 既知の課題（MVP後に対処）
1. **業務地区の分母問題** — 犯罪の分母が夜間人口のため、昼間人口の多い地区でスコアが不当に下がる。
   暫定対応として `business_area` フラグで注記を出す。本来の解決はe-Statの昼間人口を取得すること（後述）
2. **分母が小さい町丁目の発生率が発散** — 人口100人の閾値では不十分な可能性
3. **重大犯罪は3年以上あるとより安定する** — ただし遡るほど町名変更の突合コストが増える


---

## 人口データ調査結果（2026-08-06時点）

### 採用データ
東京都総務局「住民基本台帳による東京都の世帯と人口」令和7年1月 **第5表 区市町村、町丁別の世帯数及び男女別人口（人口総数）**
- 掲載ページ: `https://www.toukei.metro.tokyo.lg.jp/juukiy/2025/jy25000001.htm`（一覧は `/juukiy/jy-index.htm`）
- ファイル名: `jy25qv0500.csv`（308KB）
- **ライセンス: CC BY**
- 毎年1月1日現在で更新。犯罪データR7（2025年1年間）に対しては令和7年1月版が対応する
- 注意: 一覧ページの「時系列データ 第1〜10表」は全て区市町村/地域区分の粒度で使えない。**年度別ページの第5表だけが町丁目粒度**
- 区ごとのExcelも配布されているが、CSV版1本に23区すべて入っているのでそちらを使う

### ファイル仕様
- **文字コード: UTF-8（BOM付き）** — 犯罪データのShift_JISと異なるので注意
- 5,448行 × 8列、改行CRLF
- カラム: `地域コード` / `地域` / `町丁別地域階層` / `町丁別地域` / `世帯数(世帯)` / `人口／総数(人)` / `人口／男(人)` / `人口／女(人)`
- **区名と町丁目名が別カラム**（犯罪データは連結型なので構造が違う）
- `町丁別地域階層`: **0=区の総数、1=町丁目**。0を除外する
- 23区は地域コード `13101`〜`13123` で絞る。町丁目は3,142件
- 「人口」は日本人+外国人の総数、「世帯数」は日本人のみ/外国人のみ/複数国籍を合わせた総世帯数

### 犯罪データとの突合
**正規化なしの一致率は80.3%、以下を実装して99.5%まで改善**

1. **末尾の全角スペース除去** — 人口側は町丁目名が固定長で右側スペース埋め（`三宿1丁目　　　　`）。**これだけで約20ポイント改善する最大要因**
2. **漢数字→算用数字** — 人口側`一丁目` / 犯罪側`１丁目`
3. **全角→半角**（NFKC正規化）
4. **異体字セレクタの除去** — 人口側の大塚にU+E0104という**不可視文字**が入っている。見た目で区別できないので気づかないと詰まる
5. **異体字の変換テーブル** — `麴町`(人口側/旧字) ↔ `麹町`(犯罪側/常用漢字)

**残る不一致**
- 犯罪側のみ: 江東区中央防波堤、新宿区三栄町、渋谷区鶯谷町 など（住民ゼロ/町名変更済み）
- 人口側のみ: 大田区令和島1丁目、城南島、品川区八潮、中央区浜離宮庭園 など（埋立地・工業地帯で犯罪認知ゼロ）
  → **犯罪0件として扱えばよい**ので実質的な取りこぼしは犯罪側の数件のみ

### 表示名の注意
犯罪データ側にも末尾スペース付きの町丁目が存在する（`北沢２丁目　　　`）。
突合キーは正規化済みでも、**画面表示に使うカラムは別途trimが必要**。

---

## 実装済みプロトタイプ
- `score_proto.py` — 犯罪データ/人口データの正規化・突合・治安スコア算出。`build()` でスコア付きDataFrameを返す
- `polygon_join.py` — 町丁目ポリゴンの読み込みと人口データへの結合。`join()` でGeoDataFrameを返す
- `tokyo_flood.py` — **東京都「浸水予想区域図」の集計（洪水の主軸）**。`aggregate()` で町丁目単位の浸水深・地盤高を返す
- `takashio.py` — **東京都港湾局「高潮浸水想定区域図」の集計**。`aggregate()` で町丁目単位の高潮浸水深を返す
- `flood_join.py` — 国土数値情報の空間結合（家屋倒壊等氾濫想定区域の補完用に保持）
- `combine_scores.py` — 治安/洪水/高潮/地盤の4軸を統合（並列表示）。`build()` で全指標付きDataFrameを返す
- `schema.sql` — **Cloudflare D1 のスキーマ定義**
- `export_d1.py` — **D1投入用CSVとR2用GeoJSONを書き出すETL**
- `sumupita_scores.csv` — 全町丁目 × 全指標の算出結果

---

## バックエンド設計（D1 + R2 + GitHub Actions）

### データ量が小さいことが判明
| テーブル | 行数 | サイズ |
|---|---|---|
| towns | 3,142 | 441 KB |
| town_scores | 3,142 | 568 KB |
| crime_counts | 3,142 | 141 KB |
| hazard_details | 3,142 | 386 KB |

**D1に入るのは合計1.5MB**。無料枠5GBに対して誤差レベルで、全町丁目をメモリに載せても問題ない規模。
複雑なクエリ最適化は不要。

GeoJSONは**区ごとに24ファイル分割・6.3MB**でR2に置き、フロントは表示中の区だけ取得する。
座標は6桁（約11cm精度）に丸めてサイズ削減。

### テーブル設計の要点

**1. スコアと生値を分離する**
`town_scores` にスコア(0-100)、`crime_counts` / `hazard_details` に生の件数・浸水深を持たせる。
「治安スコア85」だけでなく「昨年の空き巣2件」「浸水深1.2m」と根拠を示せる構成。スコアだけでは納得感がない。

**2. 年度をキーに含める**
`PRIMARY KEY (key, data_year)`。来年のデータが出たら行を追加するだけ。過去比較機能も後から作れる。

**3. 「データがない」と「リスクがない」を区別する（重要）**
- `flood_covered` — 流域データの範囲外
- `tide_in_scope` — 高潮対象17区の外（世田谷・渋谷・中野・杉並・豊島・練馬）
- `scored` — 人口100人未満で算出対象外
- `has_polygon` — ポリゴン未対応（江東区海の森など）

**世田谷区の高潮スコア100は「対象区域外」であって「調査したが安全」ではない**。UIでの表現を変える必要がある。

**4. 出典テーブルを持つ**
全データがCC BYで表示義務がある。`data_sources` テーブルで管理し、フッターやスコア詳細から参照する。
ライセンス表記漏れは実害があるので、DBに持たせて機械的に出す方が安全。

**5. ポリゴン共有の表現**
`shared_polygon_key` — 大田区令和島1/2丁目、足立区入谷町/舎人町のように複数町丁目が1ポリゴンを共有する場合の参照先。
面積・人口密度を出すときの二重計上を防ぐため。

### GitHub Actions での運用
重いGIS処理（geopandas・空間結合・600万点のsjoin）は `export_d1.py` で完結させ、
成果物のCSVとGeoJSONだけをD1/R2にpushする。
**Workers側は読むだけ**になるので、Cloudflareの実行時間制限やライブラリ制約に引っかからない。

## 【重要】区平均は廃止。夜間人口を分母にした指標の限界

### 千代田区の低スコアは指標の欠陥であって実際の危険度ではない

| 区 | 町丁目数 | 居住人口 | 平均人口 | 重大率(‰) | 区平均 |
|---|---|---|---|---|---|
| **千代田区** | 81 | **67,909** | **838** | **3.0** | 28.5 |
| 新宿区 | 142 | 352,262 | 2,481 | 2.7 | 32.4 |
| 港区 | 113 | 267,583 | 2,368 | 1.9 | 33.7 |
| 足立区 | 262 | 698,231 | 2,665 | 0.7 | 46.3 |

千代田区の居住人口はわずか6.8万人だが**昼間人口は90万人前後で13倍以上の開き**がある。
犯罪は昼間人口や来訪者に対して発生するのに、**分母には夜間人口しか入っていない**ため発生率が23区最悪になる。

千代田区の内部を見ると、住宅地は正しく評価されている:

| 町丁目 | 人口 | 重大(2年) | スコア |
|---|---|---|---|
| 四番町 | 2,939 | 1 | **86.5** |
| 一番町 | 4,100 | 3 | **76.3** |
| 三番町 | 3,807 | 3 | **72.4** |
| 神田神保町一丁目 | 1,634 | 2 | **27.0** |

区平均28.5を押し下げているのは**居住者がほとんどいない業務地区の町丁目**（平均838人）が81件中の多数を占めるため。
つまり「千代田区は治安が悪い」ではなく**「千代田区は町丁目単位で見ないと意味がない」**が正しい解釈。

### 対処: 区平均を出さない（C） + 業務地区に注記（B）

**C: 区平均をUIに出さない**
`combine_scores.py` から集計を削除。町丁目単位で見るためのアプリなので、区平均は誤解を生むだけ。
分析の途中経過としては有用だったが、千代田区・中央区・港区で必ず誤解を招く。

**B: `business_area` フラグで注記を出す**
1のとき「この地域は業務地区のため、居住者の体感とスコアが異なる可能性があります」と表示する。
`town_scores` テーブルに追加済み。

代理指標は**スコアから除外している来訪者由来の犯罪（万引き・置引き・占有離脱物横領）の人口比の上位10%**。

**【精度の限界】神田神保町一丁目(7.3‰)は検出できるが、秋葉原の外神田二丁目(0.8‰)は取りこぼす。**
人口密度も試したが、神保町は13,330人/km²で中央値付近のため判別に使えなかった（町丁目が物理的に小さいため）。
**スコア自体の補正には使わず、注記を出すためだけのフラグ**として扱う。

**本来の解決策（MVP後）**: e-Stat 国勢調査「従業地・通学地集計」の小地域データから実際の昼間人口を取得し、
分母を作り直す。ただしデータ入手と3つ目の突合が必要。

## データを組み合わせて分かったこと（横断分析）

### 1. 治安と災害リスクはほぼ完全に無相関
4軸の相関（n=2,994）:

| | 治安 | 洪水 | 高潮 | 地盤 |
|---|---|---|---|---|
| 治安 | 1.000 | 0.057 | 0.064 | 0.065 |
| 洪水 | | 1.000 | 0.530 | 0.520 |
| 高潮 | | | 1.000 | **0.856** |
| 地盤 | | | | 1.000 |

**治安と他3軸の相関は0.06前後＝実質ゼロ**。「治安の良い街＝安全な街」は成り立たない。
ゼロメートル地帯234件の治安スコア中央値は54.4で、それ以外の58.9とほぼ同じ（低地だから治安が悪いわけでもない）。

実測: **災害3軸すべて70点以上の413件のうち、治安も70点以上なのは152件（37%）だけ**。
→ 並列表示にした設計判断の裏付け。片方だけ見ると必ず見落とす。

### 2. 高潮スコアと地盤スコアは実質同じものを測っている（要検討）
相関**0.856**、決定係数73%。地盤スコアの情報の3/4は高潮スコアで説明がつく。
高潮浸水想定が「堤防決壊時に標高の低い場所が水没する」シミュレーションなので当然ではある。

→ **4軸のうち1軸が冗長**。UIを簡潔にするなら地盤を独立軸から外し、
高潮スコアの補足情報（「この町丁目は平均海抜-1.2m」）として見せる方が合理的かもしれない。
一方 洪水と地盤は0.520と中程度で、**内水氾濫は標高だけでは決まらない**ことも同時に分かる。

### 3. 罪種を選別しないと結論が真逆になる
千代田区神田鍛冶町三丁目は、単純件数ベースだと2点（最低クラス）だが治安スコアは100点。
総数8件の中身は万引き・自転車盗が大半で、住民の安全に関わる犯罪はゼロだった。
**乖離90ポイント以上の町丁目が複数存在する**。

## 進捗（2026-08-06 追記）

**スコアカードUIを実装した** — `web/`（Next.js 15 App Router + Tailwind）。`npm install && npm run dev` で動く。

- `build_web_data.py` — CSV → `web/public/data/index.json`（検索用・492KB）+ `wards/{区名}.json`（詳細・計2.2MB）。
  R2のGeoJSONを区ごと24分割する方針に合わせた。D1移行時は `Dashboard.tsx` の fetch をAPI呼び出しに差し替えるだけ
- 4軸を並列表示。総合点は出していない
- **「データなし」と「リスクなし」の区別を実装** — 高潮対象外の859町丁目（内陸6区）はスコアを表示せず「対象区域外」と明示。
  一覧のバーも満点として描かず空バー＋点線枠にした（当初は満点バーが出ていてカードと矛盾していた）
- 人口100人未満の148件は全軸「算出対象外」
- 業務地区フラグ300件に注記。台風特化リスク（洪水70+ かつ 高潮30-）46件に警告を表示
- 各軸を開くと生の犯罪件数・浸水深・地盤高を根拠として表示。CC BY出典はフッターに機械出力

検証: `tsc --noEmit` 通過。全3,142件でスコアの有無とステータス判定の不整合ゼロ、対象外6区の高潮曝露が0でないケースもゼロ。

### ETLスクリプトを回収した（同日）
`score_proto.py` / `tokyo_flood.py` / `takashio.py` / `polygon_join.py` / `export_d1.py` を復帰。
**`flood_join.py` だけ未回収**（国土数値情報の空間結合。家屋倒壊等氾濫想定区域の補完用で、
`combine_scores.py` はimportしていないため現状はブロッカーではない）。

- 入力パスが `/mnt/user-data/uploads` 直書きで既に存在しない場所を指していたため、`paths.py` に集約した。
  既定は `data/`、`SUMIPITA_DATA` で上書き可。ファイルが無いときは何をどこに置くか出るエラーにしてある
- 全スクリプトの構文チェックと、`score_proto.build()` のエラーパスを確認済み
- `README.md` / `requirements.txt` を追加。生データ5種の取得元URLと実行順を記載

**生データ（R7.csv / R6.csv / jy25qv0500.csv / shinsui_*.csv / shp/ / takashio/）は未配置。**
再配布できないので各自ダウンロードが必要。`sumupita_scores.csv` は算出済みなのでフロントは動く。
**地図表示には生データの配置 → `export_d1.py` 実行が必須**（現CSVに緯度経度カラムがない）。

### ETLを通しで再実行した（同日）
生データ（R7/R6/人口/浸水7流域/ポリゴンshp/高潮shp）を配置し、パイプラインを最初から実行。
**再生成した `sumupita_scores.csv` は既存ファイルと全カラム完全一致**（差分ゼロ）。再現性を確認できた。
所要時間は `combine_scores.py` が27秒、`export_d1.py` が4秒。想定していたより遥かに軽い。

- ポリゴン紐付け 99.90%（3,139/3,142）。未解決は江東区海の森1〜3丁目のみ（人口0）
- `dist/` に D1投入用CSV 4本 + 区ごと24分割のGeoJSON（6.3MB）を出力

**見つけた不具合2件**

1. `score_proto.py` の `__main__` が `KeyError: 'burglary'` で落ちていた（`burglary_r7` へのリネーム漏れ）。
   ついでに区別平均の出力も削除した（廃止と決めた指標がスクリプトに残っていた）
2. **本メモの「4軸すべて良好 152件」は改訂前の治安スコアの値だった** → 129件に訂正済み（該当箇所参照）

`export_d1.py` は既定で `sumupita_scores.csv` を再利用するようにした（`--rebuild` で作り直す）。
GeoJSONだけ出し直すのに27秒待つ必要がなくなった。

## 地図を実装した（同日）

MapLibre GL JS + 地理院タイル（淡色地図）。`web/src/components/MapView.tsx`。

- **軸タブで塗り分けを切り替える**方式にした。4軸を小さな地図で並べる案もあったが、
  1つあたりが小さくなり町丁目の粒度が潰れるため却下
- 色は0-100の連続補間。しきい値で色を切るとスコアカードの「良好/平均的/注意」と二重基準になる
- **「データなし / 対象区域外」は斜線ハッチングで塗りと明確に分ける。** これが地図で最も事故りやすい箇所で、
  null を0点として塗ると「最悪の町丁目」に見え、逆に満点として塗ると「安全」に見える。どちらも誤り。
  実装上は**値が無い軸のプロパティ自体をGeoJSONに持たせず**、`["!", ["has", axis]]` で判定している
  （null値を持たせると expression 側で数値0に落ちる事故が起きる）
- クリックで町丁目を選択 → スコアカードと連動。検索リストからの選択でも地図が追従する

検証: 全3,139フィーチャがindexと突合（欠落・重複ゼロ）。
高潮の斜線1,004件が「対象外6区859件 + 人口100人未満145件」と集合として完全一致することを確認。
`tsc --noEmit` と `next build` 通過。

**制約**: ポリゴン6.3MBを一括ロードしている。本番ではR2配信かベクトルタイル化が必要。
背景の地理院タイルは大量アクセス時に国土地理院への申請がいるので、公開前に規約確認のこと。

## 次回やること
- Workers APIの実装（D1にdist/のCSVを投入）
- ポリゴン配信をR2に移す（現状はpublicから6.3MB一括）
- `flood_join.py` の回収（家屋倒壊等氾濫想定区域）
- 荒川・多摩川の補完方法を決める
- 荒川・多摩川の補完方法を決める（ファイル分割 or 諦めて注記）
- 家屋倒壊等氾濫想定区域: 東京都はPDF/JPGのみの可能性が高く、国土数値情報から補完する方針（`flood_join.py` で実装済み・619件抽出）


---

## 高潮浸水想定区域データ（2026-08-06時点）

### 採用データ
東京都港湾局「高潮浸水想定区域図［想定最大規模］（浸水深）」shpデータ
- カタログ: `https://catalog.data.metro.tokyo.lg.jp/dataset/t000015d1700000007`
- ファイル: `shape_depth_.zip` → `shape(depth)/` に**90個のshp**、展開後345MB
- **令和6年12月改定版**（国の手引き改定に伴う最新版）
- 「浸水継続時間」版もあるが引越し先の初期判断には不要

### 想定条件（かなり厳しい）
- 我が国既往最大規模の台風（**室戸台風級：910hPa**）を想定
- 東京湾沿岸に最大の高潮を発生させる台風経路を設定
- **高潮と同時に河川での洪水を考慮**
- 最悪の事態を想定し、**堤防等の決壊を見込む**

対象は**17区**（千代田・中央・港・新宿・文京・台東・墨田・江東・品川・目黒・大田・北・荒川・板橋・足立・葛飾・江戸川）。
世田谷・渋谷・中野・杉並・豊島・練馬は区域外＝台風リスクなし。

なお同ページには**「家屋倒壊等氾濫想定区域図」も新規作成**されている。国土数値情報から補完しようとしていた指標が東京都データで手に入る可能性がある（未取得）。

### ファイル仕様
- **座標系は元から EPSG:6677（平面直角座標系IX系）**。既存の面積計算と同じなので変換不要
- 属性: `X` / `Y` / `CX` / `CY` / `DepthM`。**浸水深は実数値(m)**
- 10mメッシュのポリゴン（1セル100㎡）
- **セル中心点に変換してsjoinする**とポリゴン同士のoverlayより桁違いに速い

### 集計結果
- **1,406町丁目（44.8%）が該当。該当人口414万人**（23区人口の4割強）
- 曝露度（町丁目全体で均した平均浸水深）: 中央値1.15m / 75%点2.46m / 最大5.92m
- 上位は江東区が独占。北砂4丁目は町丁目の100%が浸水、平均5.92m・最大6.77m（人口5,581人）
  大島・亀戸・東砂・南砂と江東デルタ地帯がそのまま並ぶ

区別（該当町丁目数 / 平均曝露度 / 該当人口）:

| 区 | 件数 | 曝露度 | 人口 |
|---|---|---|---|
| 墨田区 | 104 | 2.89m | 287,302 |
| 江戸川区 | 196 | 2.76m | 693,472 |
| 葛飾区 | 155 | 2.52m | 469,916 |
| 江東区 | 155 | 2.46m | 541,685 |
| 足立区 | 165 | 1.13m | 454,136 |

**江東5区が上位5位を独占**。「江東5区大規模水害ハザードマップ」が別途作られている理由が数値で裏付けられた。
一方 中央区は89町丁目該当だが曝露度0.25m、千代田0.38m、港0.42mと軽微。**該当有無より深さが重要**。

---

## スコア設計（確定版・4軸並列）

治安 / 洪水 / 高潮 / 地盤 の**4軸を合成せず並列表示**する。

### 洪水と高潮は統合してはいけない

| | 洪水 | 高潮 |
|---|---|---|
| 中央値 | 0.15m | **0.00m** |
| 90%点 | 0.77m | **2.73m** |
| 最大 | 3.20m | 5.92m |

高潮は中央値0だが90%点2.73m。**「該当しない町丁目が多数派だが、該当すると一気に深刻」**という二極分布で、
洪水の緩やかな分布とは性質が全く違う。統合するとこの二極性が消える。

### 各軸の定義
1. **治安スコア** — 前述（重大犯罪65%[暴力+住宅侵入窃盗、2年合算+ベイズ平滑化] / 生活犯罪35%）
2. **洪水スコア** — `tk_flood_ratio × tk_mean_depth` ＝ 町丁目全体で均した平均浸水深(m)
3. **高潮スコア** — `ts_flood_ratio × ts_mean_depth`（洪水と同一の定義で揃えた）
   対象外の6区（世田谷・渋谷・中野・杉並・豊島・練馬）は曝露0として扱う
4. **地盤スコア** — 平均地盤高のパーセンタイル順位。ゼロメートル地帯は `below_sea_flag` で明示

すべて `rank(method='max', ascending=False, pct=True)*100`。人口100人未満は対象外。

### 区別 4軸スコア（※分析用の参考値。UIには出さない）

> 治安の区平均は業務地区の分母問題で歪む（千代田区の例を参照）。
> 水害系3軸は地形由来なので区平均でも傾向は読めるが、UIは町丁目単位で統一する。
> 下表の治安スコアは改訂前の値なので、現行値は本文上部の改訂後テーブルを参照。

| 区 | 治安 | 洪水 | 高潮 | 地盤 | ゼロm |
|---|---|---|---|---|---|
| 杉並区 | 67.6 | 65.0 | **100.0** | 93.3 | 0 |
| 世田谷区 | 67.1 | 53.1 | **100.0** | 82.2 | 0 |
| 大田区 | 66.5 | 50.5 | 56.1 | 42.1 | 0 |
| 江東区 | 63.6 | 41.4 | **12.6** | 14.0 | 48 |
| 葛飾区 | 60.6 | 23.3 | **12.9** | 14.1 | 58 |
| 墨田区 | 60.4 | 40.6 | **10.2** | 11.9 | 24 |
| 江戸川区 | 58.5 | 20.6 | **11.3** | 12.8 | 77 |
| 渋谷区 | 42.3 | 56.9 | **100.0** | 71.6 | 0 |

- 内陸の杉並・世田谷・練馬・中野・渋谷・豊島は高潮100.0（対象区域外）
- 江東区は治安63.6（7位）と悪くないのに水害面は最悪という組み合わせ
- 大田区は高潮56.1と中位。台地と低地の混在がここでも表れる

### 4軸にした最大の成果: 台風特化リスクの抽出
洪水スコアは良好なのに高潮が深刻な町丁目が特定できる。

| 町丁目 | 人口 | 洪水 | 高潮 | 高潮曝露 |
|---|---|---|---|---|
| 墨田区亀沢二丁目 | 2,118 | 85.3 | **11.9** | 2.4m |
| 墨田区吾妻橋三丁目 | 1,728 | 89.5 | **16.9** | 1.9m |
| 江東区東陽6丁目 | 1,990 | 87.8 | **16.4** | 2.0m |
| 港区芝浦2丁目 | 4,502 | **94.3** | **24.6** | 1.0m |

芝浦2丁目は洪水ほぼ最良なのに高潮24.6。**豪雨対策は行き届いているが堤防決壊を伴う高潮には無力**。
洪水スコアだけ見ていたら「安全」と誤判定する場所。

### 4軸すべて良好な町丁目
治安70+ / 洪水70+ / 高潮70+ / 地盤70+ をすべて満たすのは **129件・人口478,464人**（全体の約4%）。

世田谷36 / 杉並29 / 練馬29 / 目黒11 / 中野8 / 新宿5 / 渋谷4 / 品川2 / 大田2 / 千代田1 / 板橋1 / 豊島1

> **【訂正 2026-08-06】** 初出時は152件・492,476人（世田谷45 / 杉並30 / 練馬29 / 目黒14 / 中野8 / 新宿7 /
> 渋谷5 / 豊島4 / 板橋4 / 品川2 / 大田2 / 千代田1 / 港1）と書いていたが、これは**治安スコアの改訂前**
> （ベイズ平滑化を入れる前）の値だった。生データからパイプラインを再実行して現行コードの値に差し替えた。
> 平滑化により重大犯罪0件の小さな町丁目が高得点を独占しなくなったため、条件を満たす町丁目が減っている。
> 港区はゼロ件になった。

武蔵野台地の内陸区に集中。「全部入り」を求めた場合の現実的な選択肢の広さを示す指標として、アプリで提示する価値がある。


---

## 【重要】浸水データは東京都「浸水予想区域図」を主軸に採用

国土数値情報より優れている点が多く、こちらをメインに切り替えた。

### 採用データ
東京都建設局「浸水予想区域図（改定）浸水深・地盤高データ」
- カタログ: `https://catalog.data.metro.tokyo.lg.jp/dataset/t000014d0000000029`
- **ライセンス: CC BY**
- 東京都を14区域（流域）に分けてCSV提供。**23区に関係するのは7流域**
  `shinsui_kandagawa` / `shinsui_sumidagawa` / `shinsui_syakujiigawa` / `shinsui_jyounantiku` / `shinsui_koutounaibu` / `shinsui_nogawa` / `shinsui_nakagawa`
- 図郭割PDFは不要（図面の割り付け図でデータではない）
- 多摩地域の流域（鶴見川・浅川・霞川・秋川・残堀川・境川・黒目川）は23区にかからないので不要

### 国土数値情報より優れている理由
1. **外水氾濫 + 内水氾濫の両方を含む**（国土数値情報は外水のみ）。東京の低地では内水氾濫の方が被害頻度が高い
2. **浸水深が実数値（m）**。国土数値情報はランクコード1〜6で、しかも7割が「2」に潰れて識別力がなかった
3. **地盤高が同梱**されており、**ゼロメートル地帯を直接特定できる**。別途ボーリングデータを探す必要がなくなった
4. **区市町村のハザードマップと同じ原典**。住民の手元の情報と数字が一致する

### ファイル仕様
- **文字コード: UTF-8（BOM付き）**、改行CRLF
- カラム: `図郭No` / `浸水深` / `地盤高` / `緯度` / `経度`
- **約10mメッシュの規則的グリッド**（経度0.000125°≒11.3m、緯度0.00008333°≒9.26m、1セル約105㎡）
- **浸水深0の点も含まれる**。グリッドは図郭全体をカバーしており浸水域だけの抽出ではない
  → **浸水面積割合を点の比率としてそのまま計算できる**
- 7ファイル合計243MB、約600万点。メモリ3GB環境では `chunksize=400_000` で逐次sjoinが必要

### 集計結果
- 3,139町丁目のうち**3,133件（99.8%）がデータ範囲内**。7流域で23区をほぼカバー
- 浸水面積割合: 中央値0.62、25-75%が0.35〜0.86
- 浸水域内の平均浸水深: 中央値0.33m、90%点1.15m、最大3.52m

### 落とし穴: 「平均浸水深」だけ見ると低地リスクを見誤る
区別の平均浸水深は文京1.10m・杉並1.07m・新宿1.07mが上位で、江戸川0.73m・葛飾0.71mより高く出る。
これは**「浸水する場所での深さ」の平均**のため。文京・杉並・新宿は神田川流域の谷筋に局所的な深い浸水域があり、
江戸川・葛飾は広く浅く浸水する。面積割合との掛け合わせで見ると板橋・北・江戸川が正しく上位に来る。

### 残る制約
荒川・多摩川・江戸川は**国管理河川なので東京都データに含まれない**。
国土数値情報の関東地整ファイル（527MB）が必要だが、アップロード上限500MBに抑えるにはローカルで分割が要る。
ただし内水氾濫を含むため、荒川本体なしでも板橋区舟渡・北区浮間が浸水リスク上位に正しく検出できている。

---

## スコア設計（3軸版・高潮追加により4軸版に置き換え済み）

> **※この節は高潮を加える前の暫定版。確定版は上部の「スコア設計（確定版・4軸並列）」を参照。**
> 洪水・地盤・治安の定義自体は4軸版でもそのまま引き継いでいる。

### 1. 治安スコア（前述の設計を継続）

### 2. 浸水スコア
```
flood_exposure = tk_flood_ratio × tk_mean_depth
```
展開すると `depth_sum / n` になり、**「町丁目全体を均したときの平均浸水深(m)」**という物理的に明確な意味を持つ。
ランクコードから1を引いていた旧定義と違い説明が不要。

分布: 中央値0.15m / 75%点0.37m / 90%点0.77m / 最大3.20m（世田谷区鎌田1丁目）

浸水リスク上位: 世田谷区鎌田1丁目3.20m、板橋区舟渡一丁目2.98m、舟渡二丁目2.85m、北区浮間2丁目2.73m、江戸川区西葛西2丁目2.64m

### 3. 地盤スコア（独立指標）
平均地盤高 `tk_mean_elev` のパーセンタイル順位。**ゼロメートル地帯は `below_sea_flag` で明示**。

ゼロメートル地帯（平均地盤高 < 0m）: **234件 / 人口968,073人**

| 区 | 町丁目数 |
|---|---|
| 江戸川区 | 77 |
| 葛飾区 | 58 |
| 江東区 | 48 |
| 足立区 | 27 |
| 墨田区 | 24 |

ゼロメートル地帯の**平均浸水スコアは19.7**。低地と浸水リスクがきれいに相関しており指標として整合している。

### 3軸を合成せず並列表示する（確定方針）
区別の対比（治安 / 浸水 / 地盤）:

| 区 | 治安 | 浸水 | 地盤 | ゼロm地帯 |
|---|---|---|---|---|
| 杉並区 | 67.6 | 65.0 | 93.3 | 0 |
| 世田谷区 | 67.1 | 53.1 | 82.2 | 0 |
| 大田区 | 66.5 | 50.5 | **42.1** | 0 |
| 江東区 | 63.6 | 41.4 | 14.0 | 48 |
| 葛飾区 | 60.6 | 23.3 | 14.1 | 58 |
| 江戸川区 | 58.5 | 20.6 | 12.8 | 77 |
| 新宿区 | 49.9 | 70.9 | 65.0 | 0 |

- 杉並・練馬・中野・世田谷は武蔵野台地上で地盤が高い
- 墨田・江戸川・江東・葛飾は低地
- **大田区は治安4位だが地盤42.1（中位）**。台地と低地が混在するため区単位では判断できず町丁目単位が必要 — このアプリの存在価値そのもの

ギャップ抽出（治安は良いが浸水リスクが高い）:
- 葛飾区西新小岩5丁目: 治安97.5 / 浸水1.7 / **地盤-0.6m** — 治安トップクラスなのにゼロメートル地帯
- 板橋区東坂下一丁目: 治安96.7 / 浸水0.5
- 大田区池上二丁目: 治安100 / 浸水6.1

片方だけ見ていたら気づけない組み合わせが抽出できている。


---

## 洪水浸水想定区域データ（2026-08-06時点）

### 採用データ
国土数値情報「洪水浸水想定区域データ（河川単位）」**2025年度（令和7年度）版**、GeoJSON形式
- ダウンロード: `https://nlftp.mlit.go.jp/ksj/gml/datalist/KsjTmplt-A31a-2025.html`
- 東京都は2ファイル。**両方必要**（片方だけだと浸水域が抜ける）
  - `A31a-25_13_10_GEOJSON.zip`（12.05MB）洪水予報河川・水位周知河川
  - `A31a-25_13_20_GEOJSON.zip`（20.28MB）その他の河川
- **ライセンス: 2021〜2025年度版はCC BY 4.0。2012年度（平成24年度）版は「非商用」**
  → 必ず最新版を使う。古い版は水防法改正前の体系で、データの意味も異なる二重の事故になる

### ファイル仕様
- 展開すると57ファイル / 281MB。5カテゴリのフォルダに分かれる
  - `10_計画規模` / `20_想定最大規模` / `30_浸水継続時間`
  - `41_家屋倒壊等氾濫想定区域_氾濫流` / `42_家屋倒壊等氾濫想定区域_河岸侵食`
- ファイルは河川ごとに分割。1ファイル最大3万件、25MB級もある
- **座標系: EPSG:6668（JGD2011）** — 町丁目ポリゴン(JGD2000)と異なるので結合前に揃える
- 属性（想定最大規模の場合）: `A31a_201`河川番号 / `A31a_202`河川名 / `A31a_203`河川管理者番号 / `A31a_204`河川管理者 / `A31a_205`浸水深ランク(1〜6)
- **不正ジオメトリが混在している。`buffer(0)` で修復しないと `overlay` が落ちる**
- 面積計算は **EPSG:6677（平面直角座標系IX系＝東京）** に投影して実施

### 対象河川と欠落
東京都ファイルに含まれるのは15河川:
神田川、隅田川、中川、旧中川、石神井川、呑川、丸子川、境川、鶴見川、恩田川、三沢川、残堀川、秋川、霞川、黒目川

**荒川・多摩川・江戸川が入っていない**（国管理河川のため関東地方整備局ファイル `A31a-25_83_10_GEOJSON.zip`（527MB）側にある）。
江戸川区・葛飾区・大田区の評価には本来必須。→ 次回対応。

### 未確定事項
浸水深ランク1〜6の具体的な区分値（何m以上何m未満か）はコードリスト `water_depth_code.xlsx` に定義されているがZIPに同梱されていない。
順序（1が最も浅く6が最も深い）は確実なのでスコア化は可能だが、**「浸水深3m以上」等の表示をするなら要確認**。

### 重要な注意点
- **東京都は独自に「浸水予想区域図」を作成・公表**しており、区市町村のハザードマップはそちらを基にしている。
  そのため**住民が持っているハザードマップと浸水域が一致しない**。出典を明示する設計が必要
- 洪水浸水想定のシミュレーションは支川の氾濫・想定超過降雨・高潮・内水氾濫を考慮していない。
  指定区域外でも浸水しうるし、実際の浸水深がより大きい場合もある

---

## 浸水スコア設計（確定版）

### 却下した案: 浸水面積割合の単独使用
空間結合の結果、**3,139町丁目のうち3,125件（99.6%）が浸水想定区域にかかった**。
想定最大規模は「1000年に1度クラス」の降雨前提で、ランク1（最も浅い区分）が16万ポリゴンと大多数を占め、23区のほぼ全域を覆うため。
データは正しく、**間違っていたのはスコア設計の方**だった。二値では識別力ゼロ。

`max_depth_rank` も7割（2,243件）が「2」に集中して頭打ちになるため単独では使えない。

### 採用: 浸水曝露度
```
flood_exposure = flood_ratio × max(mean_depth_rank - 1, 0)
```
- `mean_depth_rank` は面積加重の平均浸水深ランク
- **1を引くのが要点**。ランク1は23区ほぼ全域が該当するため基準線（曝露ゼロ）とみなす
- 「浅い浸水が広がっているだけ」と「深い浸水が広がる」を分離できる
- 深さだけでも広さだけでも足りず、掛け合わせて初めて意味を持つ

分布: 中央値0.083 / 25%点0.012 / 75%点0.321 / 最大1.44 → 狙い通りリスク上位が分離された

正規化は治安スコアと同じパーセンタイル方式（`rank(method='max', ascending=False, pct=True)*100`）。人口100人未満は対象外も同条件。

### 家屋倒壊等氾濫想定区域は独立表示
- 該当は609件（19%）、**人口合計203万人**（23区人口の2割強）
- 浸水深より重大なリスクで、曝露度に混ぜると薄まる → スコアとは別にフラグ＋面積割合で表示
- 治安スコアが高いのに倒壊リスクを抱える町丁目が実在する（新宿区中井1丁目: 治安95.6 / 倒壊率0.55、葛飾区西新小岩5丁目: 治安97.6 / 倒壊率0.52）

### 治安スコアと浸水スコアは合成せず並列表示
引越し先選びでは「治安は良いが水害リスクが高い」という情報こそが価値。1つの数字に潰すと判断材料が失われる。

区別の対比（治安 / 浸水）:
- 葛飾区 60.6 / 29.1、江戸川区 58.5 / 23.7 — 治安中位だが浸水最下位級
- 豊島区 52.8 / 66.3、中央区 57.8 / 65.3 — 治安下位だが浸水は良好

合成していたら両者が同じ点数に潰れていた。

ギャップ抽出も実用的（江戸川区鹿骨町: 治安100 / 浸水0.9、葛飾区細田4丁目: 治安100 / 浸水3.0）。

### 算出結果の妥当性
浸水リスク上位は板橋区舟渡・北区浮間が独占。荒川沿いの低地で、荒川本体のデータ未投入でも支流（新河岸川等）から正しく検出できている。

> **※この節の国土数値情報ベースの浸水スコアは、東京都「浸水予想区域図」に主軸を切り替えたため現在は非採用。**
> 家屋倒壊等氾濫想定区域（東京都データにない指標）を補完する用途でのみ、`flood_join.py` を保持している。

---

## 浸水・洪水リスク調査結果（2026-08-06時点）

### 用途別に2つのデータ源を併用する

**1. 地図表示 → 重ねるハザードマップのタイル配信**
- 商用非商用問わず利用可。地理院タイル仕様に準拠したPNGラスタータイル
- Leaflet/MapLibreに重ねるだけで済み、実装コストが極めて低い
- **画像なので数値は取り出せない**。スコア算出には使えない
- ベースURL: `https://disaportaldata.gsi.go.jp`、ズームレベル2〜17

| レイヤー | パス |
|---|---|
| 洪水浸水想定区域（想定最大規模） | `/raster/01_flood_l2_shinsuishin_data/{z}/{x}/{y}.png` |
| 内水（雨水出水）浸水想定区域 | `/raster/02_naisui_pref_data/13/{z}/{x}/{y}.png` |
| 高潮浸水想定区域 | `/raster/03_hightide_l2_shinsuishin_pref_data/13/{z}/{x}/{y}.png` |
| 津波浸水想定 | `/raster/04_tsunami_newlegend_pref_data/13/{z}/{x}/{y}.png` |

（`13` は東京都の都道府県コード。東京都は配信対象に含まれる）
- 配信元ページ: `https://disaportal.gsi.go.jp/hazardmapportal/hazardmap/copyright/opendata.html`
- 出典表記が必要

**2. スコア算出 → 国土数値情報のGISデータ**
- 洪水浸水想定区域データ（河川単位）、最新は2025年度（令和7年度）版
- シェープファイル/GeoJSON形式のポリゴン。町丁目ポリゴンとの空間結合で「浸水想定区域の面積割合」「最大浸水深」を数値化できる
- ダウンロード: `https://nlftp.mlit.go.jp/ksj/gml/datalist/KsjTmplt-A31.html`
- 利用規約: `https://nlftp.mlit.go.jp/ksj/other/agreement.html`（未精査）

### 重要な注意点
- **東京都の公表図面では浸水深0.1m未満の区域は着色されていない**。内水浸水想定も全国統一基準のため「0m〜0.1m」は非表示
  → **「色が付いていない＝浸水しない」ではない**。スコア化でリスクゼロと誤判定しないこと

---

## 町丁目ポリゴン調査結果（2026-08-06時点）

### 採用データ
e-Stat「令和2年国勢調査 町丁・字等別境界データ」東京都
- ダウンロード: `https://www.e-stat.go.jp/gis/statmap-search?type=2`
  → 境界データ → 小地域 → 国勢調査 → 2020年 → 小地域(町丁・字等別) → 世界測地系緯度経度・Shapefile → 13 東京都
- ファイル: `A002005212020DDSWC13.zip` → `r2ka13.shp` / `.dbf` / `.shx` / `.prj`
- 5年ごと（国勢調査ごと）に更新

### ファイル仕様
- フィーチャ6,021件（都全域）。**23区の町丁目は3,173件**
- **座標系: EPSG:4612（JGD2000）** — 事前情報のJGD2011ではなくJGD2000だった。WGS84(4326)への変換が必要
- **`CITY`カラムは `13101` ではなく `101` の3桁形式**（`PREF`が`13`で別カラム）。
  5桁で絞ると0件になる — **実際にこれで一度ハマった**
- 主なカラム: `KEY_CODE`(13桁一意コード) / `CITY_NAME` / `S_NAME` / `AREA`(面積㎡) / `JINKO` / `SETAI` / `HCODE`
- `HCODE`: 8101=通常の町丁目(3,149件)、8154=水面調査区など(24件)
- **`AREA` があるので人口密度が計算できる**（別途面積計算は不要だった）

### 人口データとの突合
正規化ロジックは犯罪データと共通のものが使える。**素の一致率99.4%**。
残る19件は手動エイリアステーブル（`POLYGON_ALIAS`）で対応し、**最終的に99.90%（3,139/3,142）**。

不一致は4パターンあり、いずれも自動解決は困難なので手動マッピングが現実的:

| パターン | 例 |
|---|---|
| A. 町名変更（「神田」が冠された） | 人口`神田三崎町1丁目` ↔ ポリゴン`三崎町1丁目` |
| B. ポリゴン側が丁目未分割 | 人口`神田司町2丁目` ↔ ポリゴン`神田司町` |
| C. 複数町名が1ポリゴンに統合 | 人口`入谷町`/`舎人町` ↔ ポリゴン`入谷町,舎人町` |
| D. 異体字 | 人口`松濤1丁目` ↔ ポリゴン`松涛1丁目` |

**未解決3件**: 江東区海の森1〜3丁目（令和2年時点で未成立の埋立地）。**3件とも人口0なので実害なし**。

**共有ポリゴン2件**: 大田区令和島、足立区入谷町/舎人町。
浸水リスク判定なら同じ値を両方に割り当ててよいが、**面積・人口密度では二重計上になるので注意**。

### 副産物: 人口密度
`AREA` から人口密度を算出可能。上位は中央区勝どき4丁目（9.3万人/km²）、勝どき6丁目、湊2丁目などタワマン密集地。
→ **既知の課題「千代田区のオフィス街が満点を独占」の解決に使える**。人口密度で住宅地らしさをフィルタできる。

